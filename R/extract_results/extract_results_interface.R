

extract.multiple.simset.results <- function()
{
    
}

extract.simset.results <- function()
{
    
}

extract.sim.results <- function(sim,
                                outcomes,
                                years,
                                dimension.values,
                                keep.dimensions,
                                use.cdc.categorizations=T)
{
    
}

# MODEL -> CDC
# [msm, never_IDU] -> male, msm
# [msm, c(active_IDU, IDU_in_remission)] -> male, msm_idu
# [heterosexual_male, never_IDU] -> male, heterosexual
# [heterosexual_male, c(active_IDU, IDU_in_remission)] -> male, idu
# [female, never_IDU] -> female, heterosexual
# [female, c(active_IDU, IDU_in_remission)] -> female, idu
