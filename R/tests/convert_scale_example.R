convert.model.scale(values = c(0.4,0.5), convert.from.scale = 'proportion', convert.to.scale = 'non.negative.number', denominator.values = c(10, 20))
convert.model.scale(values = c(30, 20), convert.from.scale = 'non.negative.number', convert.to.scale = 'proportion', denominator.values = c(50, 80))
